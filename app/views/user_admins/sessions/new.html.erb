<div class="center-things">
  <div class="py-5 form-container" id='formContent'>
    <h2>Iniciar sesión</h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), class:'form-content') do |f| %>
      <div class="field text-start">
        <%= f.label 'Email', class:'form-label' %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'form-control' %>
      </div>

      <div class="field text-start mt-3">
        <%= f.label 'Contraseña', class:'form-label' %><br />
        <%= f.password_field :password, autocomplete: "current-password", class:'form-control' %>
      </div>
      
      <div class="actions mt-3">
        <%= f.submit "Iniciar sesión", class: 'btn btn-success' %>
      </div>
    <% end %>
    <div class="d-flex flex-column ms-3 text-success">
      <%= render "user_admins/shared/links" %>
    </div>
  </div>
</div>
